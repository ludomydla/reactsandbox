// Not used for the moment. Want to achieve the state, where the need for getters will be obvious

export const getUserName = (store: any) => store.username
export const getPassword = (store: any) => store.password
